# Add Prisma Model

Add a new Prisma model to schema.prisma:
- Proper field types for SQL Server
- Indexes for performance
- Relationships if needed
- Default values where appropriate
- Use @@map for table names
- Generate migration after