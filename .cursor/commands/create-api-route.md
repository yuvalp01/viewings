# Create API Route

Create a Next.js API route handler in app/api/ with:
- TypeScript types
- Zod schema validation
- Prisma database queries
- Proper error handling
- HTTP status codes
- Consistent response format
- Connection to Azure SQL Database via Prisma